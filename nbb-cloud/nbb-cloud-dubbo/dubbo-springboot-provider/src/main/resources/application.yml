server:
  port: 8100


dubbo:
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://127.0.0.1:8848?username=nacos&password=nacos
    parameters:
      namespace: dubbo

spring:
  application:
    name: dubbo-springboot-provider
